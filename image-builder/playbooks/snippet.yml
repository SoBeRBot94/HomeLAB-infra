---
- hosts: hypervisors
  gather_facts: false
  vars_files:
    - ../vars/secrets.yml

  tasks:
    - name: Enable snippets on local storage
      command:
        cmd: pvesm set local --content iso,vztmpl,snippets
      changed_when: true

    - name: Upload cloud-init config
      template:
        src: "{{ playbook_dir }}/../files/cloud-init.yml.j2"
        dest: /var/lib/vz/snippets/cloud-init.yml
        mode: '0644'
